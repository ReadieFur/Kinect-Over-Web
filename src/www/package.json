{
	"scripts": {
		"Debug": "tsc && node ./build.js --Debug",
		"Release": "tsc && node ./build.js --Release",
		"publish": "tsc && node ./build.js --Publish",
		"build": "tsc && node ./build.js --Build"
	},
	"dependencies": {
		"directory-tree": "^2.2.7",
		"log-timestamp": "^0.3.0",
		"typescript": "^4.2.3"
	}
}